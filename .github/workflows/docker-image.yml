name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: test
      run: pwd
    - name: test 2
      run: ls
    - name: Build the Docker image
      run: docker build . --tag literallyonenote:$(date +%s)

